(()=>{'use strict';document.querySelectorAll('[data-complex-details]').forEach(button=>button.addEventListener('click',()=>{const card=button.closest('article');const dialog=document.createElement('dialog');dialog.className='offer-dialog';const title=document.createElement('h2');title.id='complexOfferTitle';title.textContent=card.querySelector('h2').textContent;dialog.setAttribute('aria-labelledby',title.id);dialog.append(title,card.querySelector('.ticks').cloneNode(true));const p=document.createElement('p');p.textContent=card.querySelector('.price').textContent==='Gratuit'?'Votre demande est vérifiée par notre équipe avant publication.':card.querySelector('.price').classList.contains('dark')?'Le contenu et le tarif sont définis ensemble dans votre devis.':'Abonnement à 79,90 € / mois. Les paiements des réservations sont redirigés vers votre plateforme. La publication d’un nouveau complexe reste soumise à validation.';dialog.append(p);const close=document.createElement('button');close.type='button';close.className='offer-more';close.textContent='Fermer';close.onclick=()=>dialog.close();dialog.append(close);dialog.addEventListener('close',()=>{dialog.remove();button.focus()},{once:true});document.body.append(dialog);dialog.showModal()}))})();