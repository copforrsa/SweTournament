(()=>{
'use strict';
if(window.__SWE_PLAYER_ACTIVITY_ATTENDANCE_4393)return;window.__SWE_PLAYER_ACTIVITY_ATTENDANCE_4393=true;
const E=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const st=()=>{try{return typeof S!=='undefined'?S:null}catch(_){return null}};
const cli=()=>{try{return typeof sb!=='undefined'?sb:null}catch(_){return null}};
const admin=()=>{try{return typeof isAdmin==='function'&&isAdmin()}catch(_){return false}};
const labelStatus=s=>({expected:'À confirmer',on_time:'À l’heure',late:'En retard',absent:'Absent',late_withdrawal:'Désistement dernière minute'})[s]||'À confirmer';
let timer=0,publicBusy=false;

function styles(){
 if(E('swe4393Styles'))return;const x=document.createElement('style');x.id='swe4393Styles';x.textContent=`
 .swe-eval-summary.swe4393{grid-template-columns:repeat(4,minmax(0,1fr))}.swe-eval-missing{margin:6px 0 12px 0}.swe-eval-missing summary{cursor:pointer;color:#075985;font-weight:800;font-size:12px}.swe-eval-chips{display:flex;flex-wrap:wrap;gap:6px;margin-top:8px}.swe-eval-chips span{background:#fff7ed;border:1px solid #fed7aa;color:#9a3412;border-radius:999px;padding:4px 8px;font-size:11px;font-weight:750}.swe-eval-row4393{border-bottom:1px solid #edf1ef;padding:9px 0}.swe-eval-row4393 .swe-eval-row{border:0;padding:0}.swe-activity-pill{display:inline-flex;margin-left:7px;padding:3px 8px;border-radius:999px;font-size:11px;font-weight:850}.swe-activity-pill.active{background:#dcfce7;color:#166534}.swe-activity-pill.inactive{background:#fee2e2;color:#991b1b}.swe-activity-overview{display:flex;gap:9px;flex-wrap:wrap;align-items:center;margin:0 0 12px;padding:12px 14px;background:linear-gradient(135deg,#f0fdf4,#eff6ff);border:1px solid #bbd9ca;border-radius:15px}.swe-activity-overview b{font-size:20px;color:#063b2a}.swe-attendance{margin:0 0 14px;padding:14px;border-radius:16px;background:linear-gradient(145deg,#082f49,#064e3b);color:#fff}.swe-attendance h3{margin:0 0 4px}.swe-attendance .intro{color:#d1fae5;font-size:12px;margin-bottom:10px}.swe-attendance-row{display:grid;grid-template-columns:minmax(135px,1fr) auto;gap:10px;align-items:center;padding:10px;margin-top:7px;background:rgba(255,255,255,.1);border:1px solid rgba(255,255,255,.18);border-radius:12px}.swe-attendance-actions{display:flex;flex-wrap:wrap;gap:5px;justify-content:flex-end}.swe-attendance-actions button{min-height:34px;padding:6px 9px;font-size:11px;border:1px solid rgba(255,255,255,.32);background:#fff;color:#123b35}.swe-attendance-actions button.current{background:#facc15;color:#422006;border-color:#fde68a}.swe-public-alerts{border-left:5px solid #f59e0b!important;background:#fffaf0!important}.swe-public-alerts h2{margin:3px 0 9px}.swe-public-alert{display:flex;justify-content:space-between;gap:12px;padding:9px 0;border-top:1px solid #fde7bd}.swe-public-alert:first-of-type{border-top:0}.swe-public-alert b:last-child{color:#9a3412;text-align:right}
 @media(max-width:720px){.swe-eval-summary.swe4393{grid-template-columns:repeat(2,1fr)}.swe-eval-row4393 .swe-eval-row{grid-template-columns:minmax(95px,1fr) minmax(90px,1.2fr) auto}.swe-attendance-row{grid-template-columns:1fr}.swe-attendance-actions{justify-content:flex-start}.swe-attendance-actions button{flex:1 1 42%}}
 `;document.head.appendChild(x);
}

function renderEvaluation(){
 const s=st(),box=E('homeCoorgVotes'),card=E('homeCoorgVotesCard');if(!admin()||!s||!box||!card)return;
 const players=(s.players||[]).slice().sort((a,b)=>String(a.name).localeCompare(String(b.name),'fr'));
 const displayName=p=>{if(p.is_group_member!==false)return p.name;const host=(s.players||[]).find(x=>String(x.id)===String(p.guest_of_player_id||''));return host?p.name+' • Guest de '+host.name:p.name+' • Guest — invitant non renseigné'};
 const ids=new Set(players.map(p=>String(p.id))),reviews=(s.skillReviews||[]).filter(r=>ids.has(String(r.player_id)));
 const ever=new Set(reviews.map(r=>String(r.player_id))),never=players.filter(p=>!ever.has(String(p.id)));
 const adminId=s.session?.user?.id,adminPlayer=(s.players||[]).find(p=>String(p.id)===String(s.myLinkedPlayerId||''));
 const staff=[{user_id:adminId,label:(adminPlayer?.name||'Administrateur')+' • Président'},...(s.coorgs||[]).filter(c=>c.active!==false).map(c=>({user_id:c.user_id,label:(s.players||[]).find(p=>String(p.id)===String(c.linked_player_id||''))?.name||String(c.email||'Co-gestionnaire').split('@')[0]}))].filter(x=>x.user_id);
 const rows=staff.map(person=>{const done=new Set(reviews.filter(r=>String(r.evaluator_user_id)===String(person.user_id)).map(r=>String(r.player_id)));return {...person,count:done.size,missing:players.filter(p=>!done.has(String(p.id)))}}).sort((a,b)=>b.count-a.count||a.label.localeCompare(b.label,'fr'));
 const chips=list=>'<div class="swe-eval-chips">'+list.map(p=>'<span>'+esc(displayName(p))+'</span>').join('')+'</div>';
 card.classList.remove('hidden');box.innerHTML='<div class="swe-eval-summary swe4393"><div class="swe-eval-kpi"><b>'+ever.size+'</b><small>joueurs et guests notés</small></div><div class="swe-eval-kpi"><b>'+never.length+'</b><small>profils jamais notés</small></div><div class="swe-eval-kpi"><b>'+players.length+'</b><small>membres + invités</small></div><div class="swe-eval-kpi"><b>'+reviews.length+'</b><small>évaluations enregistrées</small></div></div>'+(never.length?'<details class="swe-eval-missing"><summary>⚠️ Identifier les '+never.length+' profil'+(never.length>1?'s':'')+' sans aucune note</summary>'+chips(never)+'</details>':'<div class="muted" style="margin:7px 0">✅ Tous les membres et invités ont au moins une évaluation.</div>')+rows.map(r=>{const pct=players.length?Math.round(100*r.count/players.length):0;return '<div class="swe-eval-row4393"><div class="swe-eval-row"><div><b>'+esc(r.label)+'</b></div><div><div class="swe-eval-bar"><span style="width:'+pct+'%"></span></div></div><div><b>'+r.count+'</b> / '+players.length+'</div></div>'+(r.missing.length?'<details class="swe-eval-missing"><summary>'+r.missing.length+' non noté'+(r.missing.length>1?'s':'')+' par '+esc(r.label)+'</summary>'+chips(r.missing)+'</details>':'')+'</div>'}).join('');
}

function decoratePlayerActivity(){
 const s=st(),list=E('playersList');if(!admin()||!s||!list)return;
 let overview=E('sweActivityOverview');if(!overview){overview=document.createElement('div');overview.id='sweActivityOverview';overview.className='swe-activity-overview';list.before(overview)}
 const members=(s.players||[]).filter(p=>p.is_group_member!==false),active=members.filter(p=>p.active!==false).length;
 overview.innerHTML='<span><b>'+active+'</b> actifs</span><span>•</span><span><b>'+(members.length-active)+'</b> inactifs</span><span class="muted">Automatique après 4 tournois consécutifs manqués. L’admin garde la main avec Réactiver / Désactiver.</span>';
 [...list.children].forEach((row,i)=>{const p=(s.players||[])[i];if(!p||row.dataset.activity4393)return;row.dataset.activity4393='1';const info=row.querySelector('.row > span:first-child')||row.querySelector('span');if(!info)return;const badge=document.createElement('span');badge.className='swe-activity-pill '+(p.active===false?'inactive':'active');badge.textContent=p.active===false?'Joueur inactif':'Joueur actif';badge.title=p.inactivity_reason||(p.activity_status_source==='automatic_missed_4'?'4 tournois consécutifs manqués':'Statut défini par l’administrateur');info.querySelector('b')?.after(badge);if(p.active===false&&p.inactivity_reason){const reason=document.createElement('div');reason.className='muted';reason.textContent='Motif : '+p.inactivity_reason;info.appendChild(reason)}});
}

function attendanceRows(){const s=st(),t=typeof currentTour==='function'?currentTour():null;if(!s||!t)return [];return (s.tPlayers||[]).filter(r=>r.registration_status!=='waitlist'&&(r.present||['absent','late_withdrawal'].includes(r.attendance_status))).map(r=>({reg:r,player:(s.players||[]).find(p=>String(p.id)===String(r.player_id))})).filter(x=>x.player).sort((a,b)=>String(a.player.name).localeCompare(String(b.player.name),'fr'))}
async function setAttendance(playerId,status){
 const s=st(),t=typeof currentTour==='function'?currentTour():null,c=cli();if(!s||!t||!c)return;let minutes=0,note=null;
 if(status==='late'){const raw=prompt('Retard prévu de combien de minutes ?','10');if(raw===null)return;minutes=Math.max(1,Math.min(240,Number.parseInt(raw,10)||0));if(!minutes)return typeof toast==='function'&&toast('Indique un retard entre 1 et 240 minutes.');}
 if(status==='late_withdrawal')note=prompt('Précision facultative sur le désistement :','')||null;
 const r=await c.rpc('admin_set_tournament_attendance_v1',{p_tournament_id:t.id,p_player_id:playerId,p_status:status,p_delay_minutes:minutes,p_note:note});if(r.error)return typeof toast==='function'&&toast(r.error.message);
 if(typeof toast==='function')toast(status==='late'?'Retard publié sur la page d’inscription ⏱️':status==='late_withdrawal'?'Désistement publié et remplaçants recalculés.':'Présence mise à jour ✅');
 if(typeof loadTournament==='function')await loadTournament();if(typeof renderAll==='function')renderAll();setTimeout(renderAttendance,80);
}
function renderAttendance(){
 const card=E('attendanceAdminCard');if(!card)return;let box=E('sweAttendanceManager');if(!box){box=document.createElement('section');box.id='sweAttendanceManager';box.className='swe-attendance';E('attendanceList')?.before(box)}
 const rows=attendanceRows();if(!rows.length){box.innerHTML='<h3>📋 Feuille de présence</h3><div class="intro">Sélectionne une compétition avec des inscrits.</div>';return}
 if(!admin()){const incidents=rows.filter(x=>['late','late_withdrawal','absent'].includes(x.reg.attendance_status));box.innerHTML='<h3>📋 Suivi des arrivées</h3><div class="intro">Seul l’administrateur confirme les présences.</div>'+(incidents.map(x=>'<div class="swe-attendance-row"><b>'+esc(x.player.name)+'</b><span>'+esc(labelStatus(x.reg.attendance_status))+(x.reg.attendance_status==='late'?' • '+Number(x.reg.delay_minutes||0)+' min':'')+'</span></div>').join('')||'<div class="intro">Aucun retard ou désistement signalé.</div>');return}
 box.innerHTML='<h3>📋 Feuille de présence & alertes terrain</h3><div class="intro">L’administrateur confirme les arrivées. Les retards et désistements sont publiés immédiatement sur le lien d’inscription.</div>'+rows.map(x=>{const status=x.reg.attendance_status||'expected';return '<div class="swe-attendance-row"><div><b>'+esc(x.player.name)+'</b><div class="intro" style="margin:3px 0 0">'+esc(labelStatus(status))+(status==='late'?' • '+Number(x.reg.delay_minutes||0)+' min':'')+(x.reg.is_substitute?' • Remplaçant':'')+'</div></div><div class="swe-attendance-actions"><button data-att-player="'+esc(x.player.id)+'" data-att-status="on_time" class="'+(status==='on_time'?'current':'')+'">✅ À l’heure</button><button data-att-player="'+esc(x.player.id)+'" data-att-status="late" class="'+(status==='late'?'current':'')+'">⏱️ Retard</button><button data-att-player="'+esc(x.player.id)+'" data-att-status="late_withdrawal" class="'+(status==='late_withdrawal'?'current':'')+'">🚫 Désistement</button><button data-att-player="'+esc(x.player.id)+'" data-att-status="absent" class="'+(status==='absent'?'current':'')+'">❌ Absent</button></div></div>'}).join('');
 box.querySelectorAll('[data-att-player]').forEach(b=>b.onclick=()=>setAttendance(b.dataset.attPlayer,b.dataset.attStatus));
}

async function renderPublicAlerts(){
 const root=E('publicView'),ctx=window.swePageViewContext,c=cli();if(!root||root.classList.contains('hidden')||!ctx?.token||!ctx?.id||!c||publicBusy)return;publicBusy=true;
 try{const r=await c.rpc('get_public_workspace_snapshot_v2',{p_token:ctx.token});if(r.error||!r.data)return;const regs=(r.data.tournament_players||[]).filter(x=>String(x.tournament_id)===String(ctx.id)&&['late','late_withdrawal'].includes(x.attendance_status));let box=E('swePublicAttendanceAlerts');if(!regs.length){box?.remove();return}const players=new Map((r.data.players||[]).map(p=>[String(p.id),p]));if(!box){box=document.createElement('section');box.id='swePublicAttendanceAlerts';box.className='sp-card swe-public-alerts';E('registrationPeople')?.before(box)}box.innerHTML='<div class="sp-eyebrow">INFO DE DERNIÈRE MINUTE</div><h2>Arrivées & désistements</h2>'+regs.map(x=>'<div class="swe-public-alert"><span><b>'+esc(players.get(String(x.player_id))?.name||'Joueur')+'</b></span><b>'+(x.attendance_status==='late'?'⏱️ Retard annoncé • '+Number(x.delay_minutes||0)+' min':'🚫 Désistement de dernière minute')+'</b></div>').join('');}finally{publicBusy=false}
}
function apply(){clearTimeout(timer);timer=setTimeout(()=>{styles();renderEvaluation();decoratePlayerActivity();renderAttendance();renderPublicAlerts()},90)}
function watchLegacyEvaluation(){const box=E('homeCoorgVotes');if(!box||box.dataset.watch4393)return;box.dataset.watch4393='1';new MutationObserver(()=>{if(!box.querySelector('.swe-eval-summary.swe4393'))setTimeout(renderEvaluation,40)}).observe(box,{childList:true})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',apply,{once:true});else apply();
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',watchLegacyEvaluation,{once:true});else watchLegacyEvaluation();
document.addEventListener('swe:rendered',apply);document.addEventListener('click',e=>{if(e.target.closest?.('[data-view="home"],[data-view="players"],[data-view="teams"]'))apply()});
setInterval(renderPublicAlerts,10000);
})();
